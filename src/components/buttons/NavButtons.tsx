import IconButton from "./IconButton";

export default function NavButtons() {
  return (
    <div className="nav-buttons">
      <IconButton name="toLeft" />
      <IconButton name="configure" />
      <IconButton name="toRight" />
    </div>
  );
}
